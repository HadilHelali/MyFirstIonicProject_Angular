import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'PhotoGallery',
  // you should change this so it can work !!!
  webDir: 'src',
  bundledWebRuntime: false
};

export default config;
